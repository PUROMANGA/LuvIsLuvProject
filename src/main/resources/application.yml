spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookstore_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: dkzkfld12#  # ← 실제 비밀번호로 변경하세요
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ← 여기 설정이 자동 테이블 생성의 핵심입니다
    show-sql: true
    properties:
      hibernate:
        format_sql: true
